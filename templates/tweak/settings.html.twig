{% extends 'base.html.twig' %}

{% block title %}Tweak Settings{% endblock %}

{% block body %}
    <h2>Tweak Settings</h2>
    <p>Manage your tweaks.</p>
    {{ form_start(settings_form) }}
    {{ form_errors(settings_form) }}
    {{ form_widget(settings_form) }}
    {{ form_end(settings_form) }}

    <hr>

    {% include 'tweak/tweak_explanation.html.twig' %}
    {% include 'tweak/dof_explanation.html.twig' %}
    {% if git_diff|default %}
        {% include 'git_diff.html.twig' %}
    {% endif %}

{% endblock %}
